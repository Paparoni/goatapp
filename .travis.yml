language: android

node_js:
  - 5.5

notifications:
  email: false

sudo: false

branches:
  only:
  - master

install:
  - npm cache clean
  - npm install
  - npm install -g cordova

script:
  - cordova prepare
  - cordova build android --release
  
release:
  - exit 0  
